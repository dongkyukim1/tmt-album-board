# TMT — Taste My Taste 🎵

> 취향을 공유하고 싶은 사람들의 커뮤니티 — **Album Board** (블라인드 음악 리뷰)

커버만 보고 들어와서, 들어봤으면 별점과 한 줄 평을 남기는 음악 취향 공유 보드.
앨범 커버·수록곡·피처링·장르·발매연도는 **Apple iTunes API에서 실시간으로** 불러옵니다.

**🔗 Live:** https://dongkyukim1.github.io/tmt-album-board/

## ✨ 핵심 기능: 블라인드 리뷰

> **내 평점·평론을 남겨야 다른 사람의 의견이 열린다.**

- 처음에는 평균 평점이 `?.?`로 잠겨 있고, 다른 멤버들의 평론은 블러 처리됩니다.
- 별점(★ 1~5) + 한 줄 평(최소 10자)을 모두 작성해야 `잠금 해제` 버튼이 활성화됩니다.
- 제출하면 평균 평점이 공개되고, 다른 사람들의 평론이 드러나며, 내 리뷰가 `YOU` 뱃지로 맨 위에 표시됩니다.
- 평가 내역은 **localStorage에 저장**되어 새로고침 후에도 유지됩니다.

## 🎧 기능

- **실시간 iTunes 데이터** — 앨범 커버(고해상도), 수록곡 + 재생시간, 피처링 아티스트, 장르, 발매연도를 JSONP로 호출 (CORS 없이 정적 호스팅에서 동작)
- **상세 모달** — Apple Music 무드의 앰비언트 블러 배경 + 큰 커버, 수록곡 전체 리스트, 피처링 칩, 블라인드 리뷰
- **정렬** — 인기순 / 막 올라온(발매연도) / 평점 높은
- **검색** — 앨범·아티스트·장르 실시간 필터
- **마우스 인터랙션** — 커서를 따라가는 3D 틸트 + 글레어, 호버 리프트, 재생 오버레이
- **로딩/에러 상태** — 스켈레톤 로딩, 실패 시 재시도

## 🎨 디자인

> Claude Design 프로젝트 `앨범 보드 디자인 개선`의 **02 · Dark Grid** 시안 기반.

- 모노톤 · Apple Music 다크 무드 — 블랙 배경, `#f5f5f7` 텍스트, 그레이 스케일 팔레트
- SF Pro / system-ui 타이포, 980px 라운드 pill 버튼, 그라디언트 타이틀
- 랭크 뱃지 + HOT 뱃지(평점 상위), 프로스티드 블러 잠금 카드

## 🚀 실행

별도 빌드 없는 정적 파일입니다. (단, iTunes API 호출을 위해 인터넷 연결 필요)

```bash
# 로컬 서버 (file:// 는 일부 기능 제약이 있어 권장)
python3 -m http.server 8000   # http://localhost:8000
```

## 📁 구조

```
index.html   # 보드 + 모달 + 블라인드 리뷰 + iTunes 동적 로딩 (HTML/CSS/Vanilla JS 단일 파일)
```

## ⚠️ 참고

- 앨범 데이터·커버·수록곡은 Apple iTunes 공개 API에서 가져오며, 저작권은 각 권리자에게 있습니다. (개인/데모 용도)
- 커뮤니티 평점·평론은 iTunes에 없으므로 데모용으로 클라이언트에서 결정론적으로 생성됩니다. (collectionId 시드 기반)
- 보드에 노출되는 앨범 목록(시안)은 큐레이션이지만, 각 앨범의 모든 메타데이터는 런타임에 API로 조회합니다.

## 📝 라이선스

MIT
